# Copy to .env.local and fill in once you've created a Supabase project.
# Until these are set, the GraphQL API falls back to the seed data in
# lib/projects.ts (see lib/supabase.ts) — the site works without them.
SUPABASE_URL=
SUPABASE_ANON_KEY=
